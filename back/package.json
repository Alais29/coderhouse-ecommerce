{
  "name": "desafio-clase-8",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf public && cd ../front/ && npm run build --prod && cp -r build ../back/public",
    "dev": "NODE_PATH=./src nodemon -L ./src/index.ts",
    "start": "NODE_PATH=./ npx tsc && node ./build/index.js",
    "build": "npx tsc",
    "lint": "eslint . --ext .ts",
    "prettier": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "migrate:db": "npx knex migrate:latest --env development",
    "rollback:db": "npx knex migrate:rollback --env development",
    "seed:db": "npx knex seed:run --env development",
    "iniciar:db": "npm run rollback:db && npm run migrate:db && npm run seed:db"
  },
  "author": "Alfonsina Lizardo",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "knex": "^0.95.10",
    "moment": "^2.29.1",
    "mysql": "^2.18.1",
    "socket.io": "^4.1.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.2",
    "@types/uuid": "^8.3.1",
    "@typescript-eslint/eslint-plugin": "^4.22.1",
    "@typescript-eslint/parser": "^4.22.1",
    "eslint": "^7.26.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}